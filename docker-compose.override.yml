services:
  nginx:
    ports:
      - "8080:80"
      - "8443:443"

  recommendation:
    volumes:
      - ./recommendation:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8001 --reload
    ports:
      - "8001:8001"
